1、课程：协同开发工具SVN的使用和js语言基础与数据类型。

2、上课内容：回顾了时序图制作需要注意的要点；学习SVN版本管理工具，了解项目研发如何协同管理代码版本；初步学习了解html，css和Javascript语言基础语法-运行原理-变量-七种数据类型：数字，字符串，数组，对象，布尔，null，undefined。
 
	a)时序图设计要点：针对功能进行设计方便研发人员直观了解功能核心。

?

	b)SVN版本管理工具使用：

		1.创建SVN文档：在文档存放位置右键SVN checkout-输入URL：https://WQ-20151107DVPZ/svn/hx/“个人号数” 帐号：学号 密码：123456。

		2.版本操作：右键文件-TortoiseSVN-Show log-账户登录-选择对应文件版本右键-Save revision to-输入保存地址/右键对应文件-属性-Subversion-Show log-(不同的打开方式，重复前面方法操作)。

	c)html：网页标签语言负责网站架构（内容），注释代码为 <!----> 。

	d)css：层叠样式表（Cascading Style Sheets,CSS)，负责网站布局，注释代码为 /**/。

	e)javascript：负责用户端交互，注释代码为 //    /**/
		1.语法基础：<script type="text/javascript" src="index.js"></script> （外联JS文件）；<script type="text/javascript"></script>。
		2.运行原理：a.嵌套在html页面运行；b.在node环境下运行（shift右键js文件地址空白-在此处打开命令窗口-输入node(tab自动生成后缀名)-空格-粘贴地址-输入“\对应js文件”-回车执行）；从上倒下执行代码。
		3.变量：定义变量时要使用var操作符（注意var是一个关键字），后跟变量名（即一个标识符），并且只能以字母，下划线，或者$开头，后面可以跟着任意的字母，数字，下划线（$）。
		4.七种数据类型：数字，字符串，数组，对象，布尔，null，undefined，通过typeof 操作符可以进行数据类型判断，具体操作示例

3、遇到的问题：

4、心得体会：